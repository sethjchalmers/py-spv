services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3003:3003"
      - "9090:9090"
    environment:
      SPVWALLET_DB__ENGINE: postgresql
      SPVWALLET_DB__DSN: postgresql+asyncpg://spv:spv@db:5432/spv_wallet
      SPVWALLET_CACHE__ENGINE: redis
      SPVWALLET_CACHE__URL: redis://redis:6379/0
      SPVWALLET_TASK__REDIS_URL: redis://redis:6379/1
      SPVWALLET_CLUSTER__COORDINATOR: redis
      SPVWALLET_CLUSTER__REDIS_URL: redis://redis:6379/2
      SPVWALLET_NOTIFICATIONS__ENABLED: "true"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3003/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: spv
      POSTGRES_PASSWORD: spv
      POSTGRES_DB: spv_wallet
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U spv"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
